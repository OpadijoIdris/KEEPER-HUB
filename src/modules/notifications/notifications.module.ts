import { Module } from '@nestjs/common';

/**
 * Bounded context: Notifications (docs/ARCHITECTURE.md §3, §4.7, §5.7).
 * Empty scaffold — layers added when this module's Phase 2 slot starts.
 */
@Module({})
export class NotificationsModule {}
