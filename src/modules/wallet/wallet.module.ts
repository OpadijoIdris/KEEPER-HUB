import { Module } from '@nestjs/common';

/**
 * Bounded context: Wallet (docs/ARCHITECTURE.md §3, §4.4, §5.4). Empty
 * scaffold — layers added when this module's Phase 2 slot starts.
 */
@Module({})
export class WalletModule {}
