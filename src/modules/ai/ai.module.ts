import { Module } from '@nestjs/common';

/**
 * Bounded context: AI (docs/ARCHITECTURE.md §3, §4.6, §5.6). Empty
 * scaffold — layers added when this module's Phase 2 slot starts.
 */
@Module({})
export class AiModule {}
