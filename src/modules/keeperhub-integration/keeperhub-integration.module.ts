import { Module } from '@nestjs/common';

/**
 * Bounded context: KeeperHub Integration (docs/ARCHITECTURE.md §3, §4.5,
 * §5.5) — the Anti-Corruption Layer around the real KeeperHub MCP API.
 * Empty scaffold — layers added when this module's Phase 2 slot starts.
 */
@Module({})
export class KeeperHubIntegrationModule {}
